import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beratensoftware.hemaiyaoffline',
  appName: 'hemaiyaoffline',
  webDir: 'dist'
};

export default config;
